import * as React from 'react';
import { LCSDPCell } from '../../redux/classes/dp-cell';

const LCSTableRow: React.StatelessComponent<LCSDPCell[]> = props => {
    return (
        <tr>
        </tr>
    )
}